apiVersion: v1
kind: Service

metadata:
  name: self-healing-service

spec:
  type: LoadBalancer

  selector:
    app: self-healing-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
